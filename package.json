{
  "name": "dpkg-library",
  "version": "1.0.0",
  "description": "A small web page for displaying dpkg packages and their dependencies",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "watch": "nodemon index.js",
    "test": "jest --verbose --coverage",
    "build:ui": "rm -rf build && cd ./frontend && npm run build --prod && cp -r build ../"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lauripalonen/dpkg-library.git"
  },
  "author": "Lauri Palonen",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/lauripalonen/dpkg-library/issues"
  },
  "homepage": "https://github.com/lauripalonen/dpkg-library#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "nodemon": "^2.0.2",
    "supertest": "^4.0.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
